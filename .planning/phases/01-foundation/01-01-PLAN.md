---
phase: 01-foundation
plan: 01
type: execute
domain: iphone-apps
---

<objective>
Verify project structure and establish SwiftUI app foundation.

Purpose: Confirm the converted project is properly configured for iOS + macOS development.
Output: Working app shell that builds and runs on both platforms.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@Buzzword Bingo/Buzzword Bingo/BuzzwordBingoApp.swift
@Buzzword Bingo/Buzzword Bingo/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify project structure and build targets</name>
  <files>Buzzword Bingo/Buzzword Bingo.xcodeproj/project.pbxproj</files>
  <action>Verify the project has SUPPORTED_PLATFORMS for iphoneos, iphonesimulator, and macosx. Confirm SWIFT_VERSION is 6.0. Confirm both iOS and macOS deployment targets are set (iOS 18.0, macOS 15.0). Run xcodebuild for both platforms to confirm builds succeed.</action>
  <verify>xcodebuild -scheme "Buzzword Bingo" -destination 'platform=macOS' build succeeds; xcodebuild -scheme "Buzzword Bingo" -destination 'platform=iOS Simulator,name=iPhone 16' build succeeds</verify>
  <done>Both macOS and iOS builds pass with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Clean up app entry and add window configuration</name>
  <files>Buzzword Bingo/Buzzword Bingo/BuzzwordBingoApp.swift</files>
  <action>Ensure BuzzwordBingoApp.swift has proper @main entry, WindowGroup scene, and platform-specific window sizing. For macOS use .windowResizability(.contentSize). Keep it minimal - no state management yet.</action>
  <verify>App launches on macOS simulator/device showing ContentView</verify>
  <done>App entry point is clean, builds, and launches on both platforms</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -scheme "Buzzword Bingo" -destination 'platform=macOS' build` succeeds
- [ ] `xcodebuild -scheme "Buzzword Bingo" -destination 'platform=iOS Simulator,name=iPhone 16' build` succeeds
- [ ] No Swift 6 concurrency warnings
- [ ] Project structure is clean (no SpriteKit remnants)
</verification>

<success_criteria>
- Both platform builds succeed
- App launches and shows placeholder ContentView
- Ready for data model implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
